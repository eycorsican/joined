```
Usage: joined <file> [-s <sep>]

A utility to join lines in a file.

Positional Arguments:
  file              target file

Options:
  -s, --sep         join separator
  --help            display usage information
```
